<div class="container">
  <slot />
</div>

<style>
  .container {
    width: min(100% - 3rem, 1180px);
    margin-inline: auto;
    padding-inline: 1rem;
  }

  @media (max-width: 768px) {
    .container {
      width: min(100% - 2rem, 1180px);
      padding-inline: 0.5rem;
    }
  }
</style>